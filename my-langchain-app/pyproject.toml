[project]
name = "my-langchain-app"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "langchain>=0.3.30",
    "langchain-openai>=0.3.35",
    "python-dotenv>=1.2.1",
]

[tool.uv]
index-url = "https://mirrors.cloud.tencent.com/pypi/simple/"
