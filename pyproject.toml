[project]
name = "python-public"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []

[tool.uv]
index-url = "https://mirrors.cloud.tencent.com/pypi/simple/"

[tool.uv.workspace]
members = ["my-langchain-app"]
